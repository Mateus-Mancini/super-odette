import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <div className="bg-primary-dark flex-1 bg-[url(/image.png)] bg-center bg-cover flex justify-center items-center">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
